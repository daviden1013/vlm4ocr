###############################################################################
# VLM APIs to make available in the UI
#
# The value must be {'openai_compatible', 'openai', 'azure_openai', 'ollama'}
# The name is the label that will be displayed in the UI.
###############################################################################
vlm_api_options:
  - value: 'openai_compatible'
    name: 'OpenAI Compatible'
  - value: 'openai'
    name: 'OpenAI'
  - value: 'azure_openai'
    name: 'Azure OpenAI'
  - value: 'ollama'
    name: 'Ollama'
###############################################################################
# PDF rendering backend. Required to process PDF uploads.
#
# vlm4ocr depends on no PDF library, so the backend is chosen here and installed
# explicitly (see services/web_app/requirements.txt). Options:
#   'pypdfium2' - PDFium. Permissive (Apache-2.0/BSD-3), no system binary. Default.
#   'pdf2image' - poppler. Requires the GPL-licensed poppler-utils system package.
#   'pymupdf'   - MuPDF. AGPL-3.0; review the license before deploying.
# "pdf_dpi" is the resolution PDF pages are rendered at, applied identically
# across backends.
###############################################################################
pdf:
  backend: 'pypdfium2'
  dpi: 200
###############################################################################
# Directory for temporary file storage. Required!
###############################################################################
temp_directory: "services/web_app/temp"
###############################################################################
# Batch processing settings
# 
# "concurrent_batch_size" limits the number of files that can be processed 
# concurrently. This is useful to avoid overwhelming the server.
# "max_files" limits the total number of files that can be uploaded.
###############################################################################
batch_processing:
  concurrent_batch_size: 4
  max_files: 100
###############################################################################
# File upload settings
#
# "max_size_mb" limits the size per request. For single file processing, 
# this limits the file size. For batch processing, this limits the total size 
# of all files.
###############################################################################
file_upload:
  max_size_mb: 100